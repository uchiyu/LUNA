<table>
  <thead>
    <tr>
      <th> student_num </th>
      <th> name </th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
      <th><%= user.student_num %></th>
      <th><%= user.name %></th>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to "資料一覧ページへ", controller: "documents", action: "index"%>
